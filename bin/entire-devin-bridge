#!/usr/bin/env python3
"""Hook transport for Devin's cloud sessions."""

import os
import sys

sys.path.insert(
    0, os.path.join(os.path.dirname(os.path.realpath(__file__)), os.pardir, "tools")
)

from entire_agent_devin.bridge import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main(sys.argv[1:]))
